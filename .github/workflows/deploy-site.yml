name: Deploy site

on:
  push:
    branches: [master]
    paths:
      - "web/**"
      - "data/**"
      - ".github/workflows/deploy-site.yml"
  # The update-data job commits refreshed data with GITHUB_TOKEN, and such
  # pushes do NOT trigger other workflows — so redeploy when it finishes.
  workflow_run:
    workflows: ["Update player data"]
    types: [completed]
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    # For workflow_run triggers, only deploy if the data refresh actually succeeded.
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install & build
        working-directory: web
        run: |
          npm ci
          npm run build

      - name: Bundle data into the build
        run: |
          mkdir -p web/dist/data
          cp -r data/* web/dist/data/ || echo "no data yet"

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
